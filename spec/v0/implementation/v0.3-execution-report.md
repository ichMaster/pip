# Phase v0.3 — Execution Report

**Date:** 2026-06-20
**Branch:** main
**Label:** pip::phase:v0.3
**Executed by:** Claude Code
**Source:** [v0.3-issues.md](v0.3-issues.md) · [v0.3-github-report.md](v0.3-github-report.md)

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

All v0.3 issues are implemented, validated, committed, pushed, and closed on GitHub.

## Issues

| # | PIP ID | Title | Phase | Status | Commit | Files | Smoke |
|---|--------|-------|-------|--------|--------|-------|-------|
| 1 | PIP-008 | Width-aware framing primitives (box / columns) | v0.3 | completed | ded935a | 1 | pass |
| 2 | PIP-009 | Welcome banner on startup | v0.3 | completed | 8f07bda | 2 | pass |
| 3 | PIP-010 | Width-aware footer + reflow on resize | v0.3 | completed | f6278e0 | 2 | pass |
| 4 | PIP-011 | Docs — refresh UI samples (GUIDE_uk / README / CLAUDE) | v0.3 | completed | 62a6779 | 3 | pass |

## Detailed Results

### PIP-008: Width-aware framing primitives (box / columns)

**Status:** completed · **Commit:** ded935a · **GitHub:** [#8](https://github.com/ichMaster/pip/issues/8)
**Files changed:**
- `creature/render.py` (`box`, `columns`, `_fit`, `_visible_len`, `_title_border`)

**Validation:**
- [x] py_compile / import: pass
- [x] `box` lines all exactly `width` cols; correct border; truncate/pad; title in border
- [x] `columns` aligns side by side, equal rows/width; colored frame width-correct (ANSI ignored)
- [x] pure / stdlib-only; faces/`status_line`/`STATUS_HEIGHT` unchanged; smoke exit 0

---

### PIP-009: Welcome banner on startup

**Status:** completed · **Commit:** 8f07bda · **GitHub:** [#9](https://github.com/ichMaster/pip/issues/9)
**Files changed:**
- `creature/render.py` (`welcome_banner`, `BANNER_MIN_WIDTH`)
- `creature/chat.py` (`_term_width`, `_draw_intro`; `run()` uses the banner)

**Validation:**
- [x] py_compile / import: pass
- [x] PTY (100 cols): framed two-column banner (greeting | commands) renders; footer follows
- [x] Non-TTY/narrow → plain fallback (no frame); smoke exit 0
- [x] `shutil.get_terminal_size()` in chat.py; render stays width-fed/pure

---

### PIP-010: Width-aware footer + reflow on resize

**Status:** completed · **Commit:** f6278e0 · **GitHub:** [#10](https://github.com/ichMaster/pip/issues/10)
**Files changed:**
- `creature/render.py` (`status_line(..., width=...)`)
- `creature/chat.py` (`_status_width`, `_current_status`; redraw paths thread width)

**Validation:**
- [x] py_compile / import: pass
- [x] Width sweep: None/24/10 → 10-cell bars (clamped); 100 → 30; always 4 lines
- [x] **PTY resize**: 100 cols → 30-cell bars, resized to 40 cols → reflowed to 19-cell bars in place
- [x] Back-compat (`width=None`); footer stays 4 lines / `STATUS_HEIGHT` unchanged; non-TTY plain log; smoke exit 0
- [x] No curses/alternate-screen; two-clock split intact

---

### PIP-011: Docs — refresh UI samples (GUIDE_uk / README / CLAUDE)

**Status:** completed · **Commit:** 62a6779 · **GitHub:** [#11](https://github.com/ichMaster/pip/issues/11)
**Files changed:**
- `GUIDE_uk.md` (§3.2 framing primitives + §3.6 banner/reflow; v0.3→v0.4 thinking-seam fix)
- `README.md` (welcome-banner sample + width-awareness note; v0.3→v0.4 fix)
- `CLAUDE.md` (render/chat rows + Rendering note; v0.4 fix)

**Validation:**
- [x] Docs-only; no code touched; samples generated from real output
- [x] Smoke-test pass (exit 0, defensive)

## Acceptance vs. ROADMAP (v0.3 Definition of Done)

> *"startup shows a framed, colored welcome banner; panels are boxed and aligned; resizing the terminal reflows the frame (boxes fit the width, no smearing); piped/non-TTY output degrades to a plain log; the two-clock split and footer behavior are unchanged."*

- [x] Framed, colored welcome banner at startup (PTY-verified: greeting | commands panels, boxed & aligned).
- [x] Panels boxed and aligned — `box`/`columns` produce width-exact frames (every line equal width; colored frames measured via `_visible_len`).
- [x] Resizing reflows the frame — PTY test: footer bars went 30→19 cells in place on a 100→40 resize, no smearing; width recomputed each redraw.
- [x] Piped/non-TTY degrades to a plain log — banner + footer fall back; smoke-test stays clean (exit 0, no escape soup).
- [x] Two-clock split and footer behavior unchanged — body/pet/brains untouched; footer stays 4 lines (`STATUS_HEIGHT`), v0.1 in-place redraw intact; render stays pure (width fed in); no `curses`/alternate-screen.

## Release

Phase complete (4/4). **No version bump performed** — per the skill, the release is the user's call.
Suggested: `/release-version 0.3.0`.

## Next Steps

- Optionally cut `0.3.0` via `/release-version`.
- Next phase: **v0.4 — Async brain (off-thread)** ([ROADMAP.md](../../ROADMAP.md)) — run the LLM call on a background thread, keep the body ticking, and show the v0.2 `thinking` face in the v0.1 stable picture (now also framed, v0.3) while a reply is pending.
